

<%= raw sublime_video(@video.file) %>
<%= link_to @video.file, "#{@video.file}" %><br>
	<%= link_to "delete", video_path(@video), method: :delete if @video.user == current_user %><br>
	
<%= @video.file.current_path %><br>
<%= @video.screenshot %>
	