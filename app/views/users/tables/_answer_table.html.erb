<table class="table table-condensed table-borderless">
	<tr class="grey">
		<td>
			<a href="#" data-toggle="tooltip" id='answer-correct' title="Green if accepted as correct">
				<i class="icon-ok-sign"></i>
			</a>
		</td>
		<td>
			<a href="#" data-toggle="tooltip" id='answer-text' title="The Content">
				<i class="icon-align-justify"></i>
			</a>
		</td>
	</tr>
	<% answers.each do |answer| %>	
	    <tr>
			<td>
				<div class="<%= answer.correct == true ? 'green-tick' : 'default-tick' %> center">
					<i class='icon-ok-sign'></i>
				</div>
			</td>
	      <td class='left'>
			<%= link_to "#{url_for(answer.question)}#answer_#{answer.id}" do %>
				<%= strip_links(truncate(answer.body, length: 200)) %>
			<% end %>	
		  </td>
	    </tr>
	<% end %>
</table>