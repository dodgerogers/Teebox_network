<%= form_for @answer, remote: true do |f| %>

	<% if @answer.errors.any? %>
<div id="error_explanation">
	<h2><%= pluralize(@answer.errors.count, "error") %> prohibited this post from being saved:</h2>

	<ul>
		<% @answer.errors.full_messages.each do |msg| %>
  		<li><%= msg %></li>
			<% end %>
		</ul>
	</div>
	<% end %>
	
	<%= f.hidden_field :question_id, value: question_id %>

	<div class="field">
		<%= f.cktext_area :body, id: "answer_textarea"  %>
	</div><br>
	
	<%= f.submit "Save Answer", class: "btn" %>
	
	<a href="#" id="hide-text">Hide text</a>
	<% end %>