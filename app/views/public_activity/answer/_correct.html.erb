<% if activity.trackable %>
	<%= link_to activity.owner.username, activity.owner %> marked your answer as correct 
	<%= link_to raw(activity.trackable.question.title), activity.trackable.question %><br>
	<small>Created <%= time_ago_in_words(activity.trackable.created_at) %> ago</small>
<% else %>
	It has since been undone
<% end %>
