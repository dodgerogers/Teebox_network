<% meta_title("Popular Questions") %>

<%= render template: "shared/teebox_banner" %>

<%= render partial: "shared/nav_links" %>

<section class="row" id="questions-index">
	<%= render partial: "questions/questions", locals: { questions: @popular } %>
</section>