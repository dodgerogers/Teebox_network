<%= render template: "shared/teebox_banner" %>

<%= render partial: "shared/tag_cloud", locals: {tags: @decorator.tags, decorator: @decorator} %>

<%= render partial: "shared/links", locals: { decorator: @decorator } %>

<%= render partial: "questions/questions", locals: {questions: @decorator.unanswered_questions}%>