<%= content_for :head do %>
<title>Highest Votes</title>
<% end %>

<%= render template: "shared/teebox_banner" %>

<%= render partial: "shared/nav_links", locals: {tags: @decorator.tags, decorator: @decorator} %>

<%= render partial: "questions/questions", locals: {questions: @decorator.votes_questions}%>